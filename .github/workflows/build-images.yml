name: Build sandbox images

# Builds the cleaned Swagstore images that learners should NOT have to build
# themselves (paymentservice is a slow multi-stage Maven build; loadgenerator's
# upstream image was unavailable) and publishes them to GHCR so the Kubernetes
# manifests can just pull them. The `frontend` image is intentionally NOT built
# here — the RUM challenge requires learners to edit and rebuild it locally.

on:
  push:
    branches: [main]
    paths:
      - 'src/paymentservice/**'
      - 'src/loadgenerator/**'
      - 'src/frontend/**'
      - '.github/workflows/build-images.yml'
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  # The frontend is built locally by learners (the RUM challenge rebuilds it), and
  # the lab uses the classic Docker builder (DOCKER_BUILDKIT=0) so the image lands
  # in minikube's daemon. Validate the Dockerfile builds that exact way.
  validate-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build frontend with the classic builder (as the lab does)
        run: DOCKER_BUILDKIT=0 docker build -t frontend-test src/frontend

  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: [paymentservice, loadgenerator]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v6
        with:
          context: src/${{ matrix.service }}
          push: true
          tags: ghcr.io/ricardoreaddog/${{ matrix.service }}:sandbox
          labels: |
            org.opencontainers.image.source=https://github.com/Ricardoreaddog/sandbox-lab
